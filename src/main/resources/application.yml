spring:
  application:
    name: sumda
  output:
    ansi:
      enabled: always
  profiles:
    active: local
    include: my
  config:
    import: "application_secret.yml"

#  datasource:
#    url: jdbc:postgresql://${DB_URL}:5432/sumda
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: none  # ?? ????? ??, ??????? ??? ??
#    show-sql: true
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:mysql://localhost:3306/sumda_db
    username: tina
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

